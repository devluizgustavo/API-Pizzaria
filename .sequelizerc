const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'), //Caminho das configs
  'models-path': resolve(__dirname, 'src', 'models'), //Caminho dos models
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), //Alterações que serão feitas na base de dados (Historico de alterações)
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'),
};
